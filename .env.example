# Google Apps Script Web App URL for RSVP form submissions.
# Create a script that accepts POST body and appends to a sheet with headers:
# Timestamp | Full Name | Contact | Attendance | Guest Count | Meal Preference | Notes
NEXT_PUBLIC_RSVP_ENDPOINT=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
